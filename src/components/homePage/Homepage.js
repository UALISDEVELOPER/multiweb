import React from 'react';

//components
import CarouselComponent from './Carousel';

const Homepage = () => {
    return (
        <div>
            <CarouselComponent/>
        </div>
    );
};

export default Homepage;